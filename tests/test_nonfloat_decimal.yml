- case: test_nonfloat_decimal
  main: |
    from decimal import Decimal
    Decimal("1.02")
    Decimal(Decimal("1.02"))
    Decimal(1)
    Decimal(1.02)
    Decimal()

  out: |
    main:5: error: Invalid type passed to Decimal (expected "Union[int, str, Decimal]"), got float instead
